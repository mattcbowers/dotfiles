# Matt's Awesome Rprofile

local({
  r <- getOption("repos")
  #r["CRAN"] <- "http://streaming.stat.iastate.edu/CRAN"
  r["CRAN"] <- "https://cloud.r-project.org/"
  options(repos = r)
})

# beep when throwing errors
options(error=utils::alarm) # need to tell R where to find alarm()

# Change colors when running R in the terminal
#if (Sys.getenv("TERM") == "xterm-256color") library("colorout")

cat("Welcome back to R, Matt!\n\n")
